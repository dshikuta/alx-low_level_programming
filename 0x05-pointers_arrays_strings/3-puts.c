#include "main.h"

/**
 * _puts - prints a string, followed by a new line, to stdout
 * @str: string to point
 */

void _puts(char *str)
{
	int i;

	while (*str[i] != '\0')

	{

		i++;

	}

	i--;

	while (*str[i] != '\0')

	{

		_putchar(s[i--]);

	}

	_putchar('\n');
}
